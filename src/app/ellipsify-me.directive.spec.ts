import { EllipsifyMeDirective } from './ellipsify-me.directive';

describe('EllipsifyMeDirective', () => {
  it('should create an instance', () => {
    const directive = new EllipsifyMeDirective();
    expect(directive).toBeTruthy();
  });
});
